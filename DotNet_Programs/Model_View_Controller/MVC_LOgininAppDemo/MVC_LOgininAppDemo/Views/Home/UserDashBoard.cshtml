@model MVC_LOgininAppDemo.Models.UserProfile


@{ 
    ViewBag.Title = "UserDashBoard";
}

<fieldset>
    <legend>
        User DashBoard
    </legend>
    @if (Session["Username"]!= null)
            {
                <text>
                     Welcome @Session["Username"].ToString()
                </text>

            }
</fieldset>
